/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import NavigatorController from './js/menu'
import {AppRegistry} from 'react-native';
AppRegistry.registerComponent('Home', () => NavigatorController);
